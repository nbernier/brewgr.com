@{
    if (Request.QueryString["iframe"] == "1") { this.Layout = "~/Views/Shared/_LayoutNoShell.cshtml"; }
    ViewBag.Title = "How to Make Homebrew Calculations";
    ViewBag.Description = "How to make homebrew calculations.";
    ViewBag.Canonical = "https://brewgr.com/calculations";
}

<div class="calculations container view">
    <div class="row">
        <div class="col-md-3">
            @Html.Partial("_CalculationsNavigation")
        </div>
        <div class="col-md-9">
                <h3>Homebrew Calculations</h3>

                <p>Homebrew calculations can be very complicated. We have done all the hard work for you. Just plug-in your ingredients and Brewgr will make all the difficult calculations so you can focus on crafting great beer. If you are curious how Brewgr makes it's calculations please select a calculation below:</p>

                <ul class="maincalcs">
                    <li><h3><a href="@Url.Action("CalculationsOriginalGravity", "content", null, "http")@if (Request.QueryString["iframe"] == "1"){<text>?iframe=1</text>}">Original Gravity</a></h3>Origianl Gravity is the specific gravity of your wort before fermentation.</li>
                    <li><h3><a href="@Url.Action("CalculationsFinalGravity", "content", null, "http")@if (Request.QueryString["iframe"] == "1"){<text>?iframe=1</text>}">Final Gravity</a></h3>Final Gravity is the specific gravity of your wort after fermentation has completed.</li>
                    <li><h3><a href="@Url.Action("CalculationsSRM", "content", null, "http")@if (Request.QueryString["iframe"] == "1"){<text>?iframe=1</text>}">SRM/Beer Color</a></h3>Standard Reference Method/SRM/Beer Color is the color of your homebrew.</li>
                    <li><h3><a href="@Url.Action("CalculationsIBU", "content", null, "http")@if (Request.QueryString["iframe"] == "1"){<text>?iframe=1</text>}">IBU/Hop Bitterness</a></h3>International Bittering Units, or IBU, is the measure of bitterness of homebrew that comes from the boiling of hops.</li>
                    <li><h3><a href="@Url.Action("CalculationsAlcohol", "content", null, "http")@if (Request.QueryString["iframe"] == "1"){<text>?iframe=1</text>}">Alcohol Content</a></h3>Alcohol content, measured by volume (ABV) or by weight (ABW), is the amount of alcohol that was produced in your homebrew during fermentation.</li>
                    <li><h3><a href="@Url.Action("CalculationsCalories", "content", null, "http")@if (Request.QueryString["iframe"] == "1"){<text>?iframe=1</text>}">Calories</a></h3>The number of calories in your homebrew.</li>
                </ul>
        </div>
    </div>
</div>



